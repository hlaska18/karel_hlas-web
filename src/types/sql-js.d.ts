/**
 * Balíček sql.js (stejná verze SQLite jako v prohlížeči) je jen pro testy
 * lekcí kurzu SQL – web si ho stahuje z CDN (`lib/sqljs.ts`). Vlastní typy
 * nemá; testům stačí takhle.
 */
declare module "sql.js";
